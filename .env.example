# Required
GROQ_API_KEY=gsk_...          # https://console.groq.com

# Optional — only needed if you switch embeddings to HF Inference API instead of local
HF_TOKEN=hf_...               # https://huggingface.co/settings/tokens

# Optional — override the default ChromaDB persistence directory
CHROMA_PERSIST_DIR=./chroma_db
